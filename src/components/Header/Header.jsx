import React from "react";
import Navbar from "./Navbar"; // Asegúrate de importar Navbar

const Header = () => {
  return (
    <header>
      <Navbar /> {/* El componente de la barra de navegación */}
    </header>
  );
};

export default Header;
